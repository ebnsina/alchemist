// One page per stream, filled in the browser. There is nothing to prerender: the id
// is not known at build time and the data is per-account.
export const prerender = false;
export const ssr = false;
