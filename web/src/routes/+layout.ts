// Prerendered by default: every page on this site is the same for everyone. Routes
// that cannot be — anything under /api — opt out for themselves.
export const prerender = true;
export const trailingSlash = 'always';
