# Where the pages send signup and login. Baked in at build time, so a change here
# needs a rebuild. Must be an origin listed in the engine's ALCHEMIST_WEB_ORIGINS.
PUBLIC_ALCHEMIST_API=http://localhost:8090
